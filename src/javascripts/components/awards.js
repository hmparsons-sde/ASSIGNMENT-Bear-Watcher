// import bears from '../helpers/data/bearData';
// import { buildABear, printToDOM } from '../helpers/river';

// const groupByStanding = () => {
//   let listOfBears = groupBy(bears, 'fishes');
//   let goldBears = Object.values(listOfBears)[Object.values(listOfBears).length - 1];
//   let silverBears = Object.values(listOfBears)[Object.values(listOfBears).length - 2];
//   let bronzeBear = Object.values(listOfBears)[Object.values(listOfBears).length - 3];
//   let domString = `<div class='align-middle p-4 mb-5'>

//                   </div>`
// }
